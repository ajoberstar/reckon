plugins {
    id 'org.ajoberstar.defaults' version '0.7.2'
}

allprojects {
  plugins.withId('java') {
    model {
        bintray {
            owner = 'ajoberstar'
            repo = 'maven'
            pkg = 'reckon'
        }
    }
  }
}
