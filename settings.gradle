import java.nio.file.Files

Files.list(rootDir.toPath())
    .filter { path -> Files.isDirectory(path) }
    .map { path -> path.fileName.toString() }
    .filter { !it.startsWith('.') && it != 'gradle' }
    .forEach { projectPath -> include(projectPath) }
